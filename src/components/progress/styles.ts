import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    flex: 1,
    marginHorizontal: 4,
    flexDirection: 'row',
    backgroundColor: '#FFF',
    height: 2,
  },
  progress: {
    backgroundColor: 'red',
  },
});

export default styles;
